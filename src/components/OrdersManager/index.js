import React from "react";

export default function ordersManager() {

    {/* ORDEN
      items
      costo
      mesa donde se pidio o takeout
      status - ongoing / ready(past)
      fecha / start - end
    */}

  return (
    <h2>Orders Manager / View</h2>
  );
};